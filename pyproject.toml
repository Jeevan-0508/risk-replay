[project]
name = "risk-replay-backend"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Jeevan-0508", email = "jeevansiddhabhaktula@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "hypothesis>=6.168.0",
    "pydantic>=2.13.5",
    "pytest>=9.1.1",
    "sqlalchemy>=2.0.54",
    "uvicorn[standard]>=0.53.0",
]

[project.scripts]
risk-replay-backend = "risk_replay_backend:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
